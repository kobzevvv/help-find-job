# =============================================================================
# Staging Environment Configuration (PUBLIC - Safe to Commit)
# =============================================================================
# 
# This file contains the PUBLIC staging bot configuration that all developers
# can use to test against the shared staging environment.
# 
# Usage: cp env.staging.example .env (for testing staging locally)
# =============================================================================

# -----------------------------------------------------------------------------
# PUBLIC STAGING BOT (Safe to share)
# -----------------------------------------------------------------------------

# Staging bot token - this is intentionally public for open-source collaboration
# Bot: @job_search_help_staging_bot
# URL: https://t.me/job_search_help_staging_bot
TELEGRAM_BOT_STAGING_TOKEN=8358869176:AAGo9WKrpUnbLBD-Zq40DIPpfdoBZroPVfI

# Staging webhook secret (public - for testing only)
WEBHOOK_SECRET=3c8a4efeb4b36eed52758eb194300a89d3074567299b3a826ea0922100a16752

# -----------------------------------------------------------------------------
# DEVELOPER CREDENTIALS (You need to provide these)
# -----------------------------------------------------------------------------

# Your OpenAI API Key (required for AI analysis)
# Get it from: https://platform.openai.com/api-keys


# OpenAI model to use (default: gpt-4)
OPENAI_MODEL=gpt-4

# Max tokens for GPT requests (default: 1500)
MAX_TOKENS=1500

# GPT temperature (default: 0.3)
GPT_TEMPERATURE=0.3

# Session timeout in hours (default: 24)
SESSION_TIMEOUT_HOURS=24

# Max file size in MB (default: 10)
MAX_FILE_SIZE_MB=10

# Rate limit per minute (default: 10)
RATE_LIMIT_PER_MINUTE=10

# Enable debug logging (default: false)
DEBUG_LOGGING=true

# =============================================================================
# TESTING INSTRUCTIONS
# =============================================================================
# 
# 1. Copy this file: cp env.staging.example .env
# 2. Add your OpenAI API key and Cloudflare Account ID above
# 3. Test locally: npm run dev
# 4. Your local worker will use the shared staging bot
# 5. All developers share the same @job_search_help_staging_bot for testing
#
# =============================================================================
